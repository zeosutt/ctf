{% extends 'base.twig' %}

{% block body %}    
    <div class="container">
        <div class="jumbotron mt-3 mb-3">
            <h1 class="display-4">あなたの書いた記事一覧</h1>
            <hr class="my-4">
            <p>各記事は URL を共有した相手に見せることができます。</p>
            <a class="btn btn-primary btn-lg" href="/articles" role="button">新しい記事を作成する</a>
        </div>
        <div class="row mt-3 justify-content-center">
            {% for article in articles %}
                <div class="card col-md-6">
                    <div class="card-body">
                        <h2 class="card-title"><a href="/articles/{{ article['article_key'] }}">{{ article['title'] }}</a></h2>
                        <div class="mb-1 text-muted">{{ article['created_at'] }}</div>
                        <p class="card-text">{{ article['abstract'] }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
